# WorldCodes 额度查询插件适配文件

基于 Zxin-Pro/astrbot_plugin_quota_checker 提交 4d367e931a98f605c34fe13d6b34beff02e87095。此文件为本地适配版，未提交上游或安装到用户的 AstrBot。

## 安装

1. 在 AstrBot 服务目录找到 data/plugins/astrbot_plugin_quota_checker/main.py，先备份原文件。
2. 用本压缩包中的 main.py 替换原文件；保留插件其他文件。
3. 在 AstrBot 插件管理中重新加载此插件，或重启 AstrBot。
4. 设置中转站地址为 https://worldcodes.online（不带 /v1），访问令牌填写用户自己的 API Key，用户信息接口路径填写 /v1/usage。
5. Token 用量接口路径、日志接口路径、额度换算除数可保留原值；/v1/usage 模式不会使用这些配置。api_user_id 和 extra_headers 通常留空。
6. 保存配置后向机器人发送 /额度。

钱包模式会显示账号钱包余额、当前 Key 的累计及今日消费和 Token。美元金额直接读取，不除以 500000；金额保留小数，以两位小数展示。消费统计只属于当前 Key，并非账号所有 Key 的合计。今日口径沿用服务端。

如果 Key 设置总额度或周期消费限额，显示该 Key 的限额，并明确提示响应未提供账号钱包余额；订阅模式显示订阅剩余额度。缺失统计显示“暂无数据”，不伪造为零。查询这些数据不需要管理员日志权限。

## 验证与限制

已通过 14 项离线验证，覆盖钱包小数/零/负余额、Key 总限额、周期限额、订阅、缺失统计、401/403/404/429/500、非 JSON、错误结构、超时及原 New API 流程。使用 AstrBot 和 HTTP 替身验证调用流程；尚未在真实 AstrBot 实例或使用真实 Key 做联网验收。

如果仍提示无法连接，需要查看 AstrBot 日志中的 HTTP 状态码或异常类型。分享日志时隐藏 Key。插件后续升级可能覆盖此适配文件，恢复备份即可回退。

## 原因

上游 main.py 的额度解析固定读取 used_quota 和 quota，并通过 int(float(...)) 转为整数。WorldCodes /v1/usage 的钱包余额是 balance，消费统计是 usage.total.actual_cost 和 usage.today.actual_cost，因此仅改路径或除数无法兼容。此适配在用户信息路径为 /v1/usage 时启用独立解析分支，原 New API 路径保留原流程。
